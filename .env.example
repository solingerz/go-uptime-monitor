# Application listening port
PORT="8080"

# Redis connection information
REDIS_ADDR="redis:6379"
REDIS_PASSWORD=""
REDIS_DB="0"

# Monitoring configuration
# Number of worker threads for check tasks
WORKER_COUNT="5"
# Time interval between each check (seconds)
CHECK_INTERVAL_SECONDS="60"
# Run interval for data aggregation tasks (minutes)
AGG_INTERVAL_MINUTES="5"
# HTTP request timeout (seconds)
REQUEST_TIMEOUT_SECONDS="15"

# Monitoring target list (JSON format)
# Example: UPTIME_TARGETS_JSON='[{"Name":"Google","URL":"https://google.com"},{"Name":"GitHub","URL":"https://github.com"}]'
UPTIME_TARGETS_JSON=''